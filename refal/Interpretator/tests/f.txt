program q;
const c=3;
var x,y,z,dnd,edf:integer;
begin
  begin
    x:=2;
    y:=3;
    dnd:=edf
  end;
  c:=4;
  edf:=true;
  dnd:=true or false;
  f:=4;
  z:=x*y;
  if not (z>0) then   if x=2 then writeln(1); else writeln(2);
    else if x=2 then writeln(3); else writeln(4);
  y:=x
end.
